name: Hello Actions
on:
  push:
branches: [main]
jobs:
say-hello:
runs-on: ubuntu-latest
steps:- uses: actions/checkout@v4- name: Print a greeting
run: echo "Hello, GitHub Actions! This ran automatically on push." 